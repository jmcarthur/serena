# Stack configuration for test repository
resolver: lts-21.25

packages:
- .

# Extra deps if needed
extra-deps: []

# Override default flag values for local packages and extra-deps
flags: {}

# Extra package databases containing global packages
extra-package-dbs: []